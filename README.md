# Migratory-Birds--Algo

This program reads a list of bird sightings represented by integers from 1 to 5, counts how many times each bird type appears, and returns the ID of the bird type that appears most frequently—choosing the smallest ID in case of a tie.
